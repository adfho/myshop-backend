# MyShop Backend

Backend API для интернет-магазина на Flask.

## 📋 Описание проекта

MyShop Backend - это RESTful API для управления каталогом товаров, корзиной, заказами и пользователями. Проект реализует полный цикл работы интернет-магазина с авторизацией, личным кабинетом и системой уведомлений.

## ✨ Основные возможности

- 🔐 **Авторизация и регистрация пользователей** с JWT-токенами
- 📦 **Каталог товаров** с фильтрацией и сортировкой
- 🛒 **Корзина покупок** с сохранением в Cookies
- 📋 **Управление заказами** с автоматическим созданием уведомлений
- 🔔 **Система уведомлений** для пользователей
- 👤 **Личный кабинет** с историей заказов
- 🖼️ **Загрузка аватаров** пользователей

## 🛠️ Технологии

- **Flask** - веб-фреймворк
- **SQLAlchemy** - ORM для работы с БД
- **Flask-Migrate** - миграции базы данных (Alembic)
- **Flask-JWT-Extended** - аутентификация через JWT
- **Flask-CORS** - поддержка CORS для фронтенда
- **itsdangerous** - подпись cookie и безопасная сериализация
- **SQLite/PostgreSQL** - база данных
- **pytest** - фреймворк для тестирования
- **black** - форматирование кода
- **ruff** - быстрый линтер

## 📦 Установка

### Требования

- Python 3.8+
- pip

### Шаги установки

1. **Клонирование репозитория**
   ```bash
   git clone <repository-url>
   cd myshop-backend
   ```

2. **Создание виртуального окружения**
   ```bash
   python -m venv venv
   
   # Windows
   venv\Scripts\activate
   
   # Linux/Mac
   source venv/bin/activate
   ```

3. **Установка зависимостей**
   ```bash
   pip install -r requirements.txt
   ```

4. **Настройка переменных окружения**
   
   ⚠️ **ВАЖНО**: В production окружении секретные ключи ОБЯЗАТЕЛЬНЫ и должны быть заданы через переменные окружения!
   
   Создайте файл `.env` на основе `.env.example`:
   ```bash
   cp .env.example .env
   # Отредактируйте .env и укажите реальные значения
   ```
   
   **Для production**: Сгенерируйте безопасные случайные ключи:
   ```bash
   python -c "import secrets; print('SECRET_KEY=' + secrets.token_urlsafe(32))"
   python -c "import secrets; print('JWT_SECRET_KEY=' + secrets.token_urlsafe(32))"
   ```
   
   Минимальные переменные окружения:
   ```bash
   FLASK_ENV=development  # или production для продакшена
   SECRET_KEY=your-secret-key-change-in-production
   JWT_SECRET_KEY=your-jwt-secret-key-change-in-production
   DATABASE_URL=sqlite:///app.db  # или PostgreSQL URL для продакшена
   ```
   
   **В production** обязательно установите:
   ```bash
   FLASK_ENV=production
   SECRET_KEY=<случайная строка минимум 32 символа>
   JWT_SECRET_KEY=<случайная строка минимум 32 символа>
   DATABASE_URL=<URL базы данных PostgreSQL>
   ```

5. **Инициализация базы данных (миграции)**
   
   Проект использует Flask-Migrate для управления схемой БД. **НЕ используйте** `db.create_all()`!
   
   Первая инициализация:
   ```bash
   flask db init
   flask db migrate -m "Initial migration"
   flask db upgrade
   ```
   
   После изменений моделей:
   ```bash
   flask db migrate -m "Description of changes"
   flask db upgrade
   ```
   
   **⚠️ ВНИМАНИЕ**: Скрипт `data_seed.py` удаляет все данные из БД! 
   Используйте только для разработки и тестирования:
   ```bash
   python data_seed.py  # Требует подтверждения 'yes'
   ```

6. **Запуск сервера**
   ```bash
   python app.py
   ```
   
   Сервер запустится на `http://localhost:5000`

## 📁 Структура проекта

```
myshop-backend/
├── app.py                 # Главный файл приложения
├── config.py              # Конфигурация (с проверкой секретов в проде)
├── models.py              # Модели базы данных (Decimal для денег)
├── data_seed.py           # Скрипт для заполнения тестовыми данными (⚠️ с предупреждением)
├── requirements.txt       # Зависимости проекта
├── pyproject.toml         # Конфигурация black и ruff
├── .env.example           # Шаблон переменных окружения
├── API.md                 # Документация API
├── routes/                # Маршруты API
│   ├── auth.py           # Авторизация и уведомления
│   ├── catalog.py        # Каталог товаров
│   ├── cart.py           # Корзина (с подписью и HttpOnly cookie)
│   ├── orders.py         # Заказы (с Decimal расчетами)
│   └── utils.py          # Вспомогательные функции
├── tests/                 # Тесты
│   ├── conftest.py       # Конфигурация pytest
│   ├── test_auth.py      # Тесты авторизации
│   ├── test_catalog.py   # Тесты каталога
│   └── test_orders.py   # Тесты заказов
├── migrations/           # Миграции БД (создаются через flask db init)
└── static/
    └── avatars/          # Папка для аватаров пользователей
```

## 🚀 Использование

### Базовый URL API
```
http://localhost:5000/api
```

### Примеры запросов

#### Регистрация пользователя
```bash
curl -X POST http://localhost:5000/api/auth/register \
  -F "first_name=Иван" \
  -F "last_name=Иванов" \
  -F "email=ivan@example.com" \
  -F "password=password123"
```

#### Вход в систему
```bash
curl -X POST http://localhost:5000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"ivan@example.com","password":"password123"}'
```

#### Получение товаров с фильтрацией
```bash
curl -X GET "http://localhost:5000/api/catalog/products?category=1&min_price=100&sort=price_asc"
```

Полная документация API доступна в файле [API.md](API.md).

## 🗄️ База данных

### Модели

- **User** - Пользователи системы
- **Category** - Категории товаров
- **Product** - Товары
- **Order** - Заказы
- **OrderItem** - Позиции заказа
- **Notification** - Уведомления пользователей

### Связи

- User → Orders (один ко многим)
- User → Notifications (один ко многим)
- Category → Products (один ко многим)
- Order → OrderItems (один ко многим)
- OrderItem → Product (многие к одному)

## 🔒 Безопасность

### Реализованные меры защиты:

- **JWT-токены** для аутентификации (Flask-JWT-Extended)
- **Хеширование паролей** с помощью werkzeug.security
- **Обязательные секреты в production**: `SECRET_KEY` и `JWT_SECRET_KEY` должны быть заданы через переменные окружения, иначе приложение не запустится
- **Подписанные cookie** для корзины (itsdangerous) - клиент не может подделать данные
- **HttpOnly флаг** на cookie корзины - защита от XSS атак
- **Secure флаг** в production - cookie передаются только по HTTPS
- **SameSite=Lax** - защита от CSRF атак
- **Валидация входных данных** на всех эндпоинтах
- **Проверка прав доступа** - пользователи видят только свои данные
- **Точные денежные расчеты** с использованием Decimal вместо float

### Настройка для production:

1. **Сгенерируйте безопасные секретные ключи** (минимум 32 символа):
   ```bash
   python -c "import secrets; print(secrets.token_urlsafe(32))"
   ```

2. **Установите переменные окружения**:
   ```bash
   FLASK_ENV=production
   SECRET_KEY=<сгенерированный ключ>
   JWT_SECRET_KEY=<сгенерированный ключ>
   DATABASE_URL=<PostgreSQL URL>
   ```

3. **Используйте HTTPS** в production (Secure флаг cookie работает только с HTTPS)

4. **НЕ запускайте** `data_seed.py` в production - скрипт заблокирован для production окружения

## 📝 API Эндпоинты

### Авторизация (`/api/auth`)
- `POST /register` - Регистрация
- `POST /login` - Вход
- `GET /me` - Данные пользователя
- `GET /notifications` - Список уведомлений
- `PUT /notifications/<id>/read` - Отметить уведомление как прочитанное
- `PUT /notifications/read-all` - Отметить все как прочитанные

### Каталог (`/api/catalog`)
- `GET /products` - Список товаров (с фильтрацией и сортировкой)
- `GET /categories` - Список категорий
- `GET /products/<id>` - Детали товара

### Корзина (`/api/cart`)
- `GET /` - Получить корзину
- `POST /add` - Добавить товар
- `POST /remove` - Удалить товар
- `POST /update` - Обновить количество

### Заказы (`/api/orders`)
- `POST /create` - Создать заказ
- `GET /my` - История заказов

## 🧪 Тестирование

### Автоматические тесты

Проект использует pytest для автоматического тестирования:

```bash
# Установите зависимости для разработки
pip install -r requirements.txt

# Запустите все тесты
pytest

# Запустите тесты с подробным выводом
pytest -v

# Запустите конкретный файл тестов
pytest tests/test_auth.py

# Запустите тесты с покрытием
pytest --cov=. --cov-report=html
```

**Структура тестов:**
- `tests/test_auth.py` - тесты авторизации и регистрации
- `tests/test_catalog.py` - тесты каталога товаров
- `tests/test_orders.py` - тесты заказов

### Ручное тестирование API

Для ручного тестирования API можно использовать:
- **Postman** - импортируйте коллекцию из документации API
- **curl** - примеры команд см. ниже
- **HTTP клиенты** - Insomnia, httpie и др.

### Пример тестирования через curl

```bash
# Регистрация
curl -X POST http://localhost:5000/api/auth/register \
  -F "first_name=Иван" \
  -F "last_name=Иванов" \
  -F "email=ivan@example.com" \
  -F "password=password123"

# Вход
curl -X POST http://localhost:5000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"ivan@example.com","password":"password123"}'

# Получение товаров (с токеном из ответа login)
curl -X GET "http://localhost:5000/api/catalog/products?category=1" \
  -H "Authorization: Bearer <your-token>"
```

## 🔍 Проверка качества кода

### Линтинг и форматирование

Проект настроен с использованием **black** и **ruff**:

```bash
# Проверка форматирования
black --check .

# Автоматическое форматирование
black .

# Проверка линтинга
ruff check .

# Автоматическое исправление проблем
ruff check --fix .
```

**Конфигурация** находится в `pyproject.toml` и `.ruff.toml`.

### Pre-commit (рекомендуется)

Для автоматической проверки перед коммитом можно настроить pre-commit hooks:

```bash
# Установка pre-commit
pip install pre-commit

# Установка хуков
pre-commit install
```

## 🌐 Развертывание

### Локальное развертывание

Проект готов к развертыванию на:
- **Render** - https://render.com
- **Railway** - https://railway.app
- **Supabase** - https://supabase.com
- **Heroku** - https://heroku.com
- **Fly.io** - https://fly.io

### Переменные окружения для продакшена

⚠️ **ОБЯЗАТЕЛЬНО** установите следующие переменные:

```bash
FLASK_ENV=production
SECRET_KEY=<сгенерированный-секретный-ключ-минимум-32-символа>
JWT_SECRET_KEY=<сгенерированный-jwt-ключ-минимум-32-символа>
DATABASE_URL=<url-postgresql-базы-данных>
```

**ВАЖНО**: В production окружении приложение **не запустится** без `SECRET_KEY` и `JWT_SECRET_KEY`!

### Пример конфигурации для Render

1. Подключите репозиторий
2. Укажите команду запуска: `gunicorn app:app` (установите gunicorn: `pip install gunicorn`)
3. Установите переменные окружения (см. выше)
4. Добавьте PostgreSQL базу данных
5. Для миграций добавьте команду Build: `flask db upgrade`

### Пример конфигурации для Railway

1. Подключите репозиторий
2. Railway автоматически определит Flask приложение
3. Установите переменные окружения в настройках проекта
4. Добавьте PostgreSQL аддон
5. Настройте команду запуска: `gunicorn app:app`

### Миграции в production

После развертывания выполните миграции:

```bash
flask db upgrade
```

Или добавьте это в команду запуска:

```bash
flask db upgrade && gunicorn app:app
```

## 📚 Документация

- [API.md](API.md) - Полная документация всех эндпоинтов
- Код содержит комментарии на русском языке

## 🐛 Известные проблемы и ограничения

### Текущие ограничения:

- **SQLite используется по умолчанию** (рекомендуется PostgreSQL для продакшена)
- **Файлы аватаров хранятся локально** (для продакшена рекомендуется использовать S3 или подобный сервис)
- **Нет автоматической очистки старых уведомлений** - требуется периодическая задача или cron job
- **Корзина хранится в cookie** - для пользователей без входа ограничено размером cookie (~4KB)

### Планы развития:

- [ ] Добавить автоматическую очистку старых уведомлений (фоновые задачи)
- [ ] Реализовать серверное хранение корзины для авторизованных пользователей
- [ ] Добавить уведомления через email/SMS
- [ ] Добавить интеграцию с платежными системами
- [ ] Реализовать систему оценки товаров и отзывов
- [ ] Добавить админ-панель для управления товарами и заказами

### Технический долг:

- Необходимо добавить rate limiting для защиты от DDoS
- Добавить мониторинг и логирование ошибок (Sentry, ELK)
- Реализовать кэширование для каталога товаров
- Оптимизировать запросы к БД (N+1 queries)

## 🤝 Вклад в проект

1. Создайте форк проекта
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Отправьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект создан в рамках учебной практики.

## 👤 Автор

Создано в рамках практики по Backend разработке.

## 📞 Контакты

При возникновении вопросов обращайтесь к куратору проекта.

---

**Версия:** 1.0.0  
**Последнее обновление:** 2024

